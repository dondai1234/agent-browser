# ~/.hermes/config.yaml
# Hermes Agent (NousResearch) - MCP servers are declared in YAML under mcp_servers.
# Reload after editing with:  /reload-mcp
mcp_servers:
  goshawk:
    command: "goshawk"
    args: ["mcp"]
